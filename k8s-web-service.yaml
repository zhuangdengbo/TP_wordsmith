apiVersion: v1
kind: Service
metadata:
  name: wordsmith-web
  labels:
    app: wordsmith-web
spec:
  selector:
    app: wordsmith-web
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
